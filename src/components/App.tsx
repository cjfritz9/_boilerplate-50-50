import React from 'react';
import * as Router from 'react-router-dom';

const App: React.FC = () => {
  return (
    <div className='App'>
      <Router.Routes>
        <Router.Route path='/' element={} />
      </Router.Routes>
    </div>
  );
};

export default App;
